.idea
.venv
virtualenv
*.pyc
dist
build
*.egg-info
upload_new_version.sh
.coverage
coverage.xml
.DS_STORE
/transcripts
.env

# Upstream test fixtures contain YouTube's public API keys that trigger
# GitHub secret scanning alerts — not needed for the pipeline.
youtube_transcript_api/test/
